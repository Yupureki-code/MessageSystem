// -*- C++ -*-
//
// This file was generated by ODB, object-relational mapping (ORM)
// compiler for C++.
//

#ifndef FRIEND_ODB_HXX
#define FRIEND_ODB_HXX

#include <odb/version.hxx>

#if ODB_VERSION != 20500UL
#error ODB runtime version mismatch
#endif

#include <odb/pre.hxx>

#include "friend.hxx"

#include "../user/user-odb.hxx"

#include <memory>
#include <cstddef>
#include <utility>

#include <odb/core.hxx>
#include <odb/traits.hxx>
#include <odb/callback.hxx>
#include <odb/wrapper-traits.hxx>
#include <odb/pointer-traits.hxx>
#include <odb/container-traits.hxx>
#include <odb/no-op-cache-traits.hxx>
#include <odb/result.hxx>
#include <odb/simple-object-result.hxx>
#include <odb/view-image.hxx>
#include <odb/view-result.hxx>

#include <odb/details/unused.hxx>
#include <odb/details/shared-ptr.hxx>

namespace odb
{
  // Friendships
  //
  template <>
  struct class_traits< ::Friendships >
  {
    static const class_kind kind = class_object;
  };

  template <>
  class access::object_traits< ::Friendships >
  {
    public:
    typedef ::Friendships object_type;
    typedef ::Friendships* pointer_type;
    typedef odb::pointer_traits<pointer_type> pointer_traits;

    static const bool polymorphic = false;

    typedef long unsigned int id_type;

    static const bool auto_id = true;

    static const bool abstract = false;

    static id_type
    id (const object_type&);

    typedef
    no_op_pointer_cache_traits<pointer_type>
    pointer_cache_traits;

    typedef
    no_op_reference_cache_traits<object_type>
    reference_cache_traits;

    static void
    callback (database&, object_type&, callback_event);

    static void
    callback (database&, const object_type&, callback_event);
  };

  // FindFriend
  //
  template <>
  struct class_traits< ::FindFriend >
  {
    static const class_kind kind = class_view;
  };

  template <>
  class access::view_traits< ::FindFriend >
  {
    public:
    typedef ::FindFriend view_type;
    typedef ::FindFriend* pointer_type;

    static void
    callback (database&, view_type&, callback_event);
  };
}

#include <odb/details/buffer.hxx>

#include <odb/mysql/version.hxx>
#include <odb/mysql/forward.hxx>
#include <odb/mysql/binding.hxx>
#include <odb/mysql/mysql-types.hxx>
#include <odb/mysql/query.hxx>

namespace odb
{
  // Friendships
  //
  template <typename A>
  struct query_columns< ::Friendships, id_mysql, A >
  {
    // id
    //
    typedef
    mysql::query_column<
      mysql::value_traits<
        long unsigned int,
        mysql::id_ulonglong >::query_type,
      mysql::id_ulonglong >
    id_type_;

    static const id_type_ id;

    // uid
    //
    typedef
    mysql::query_column<
      mysql::value_traits<
        ::size_t,
        mysql::id_ulonglong >::query_type,
      mysql::id_ulonglong >
    uid_type_;

    static const uid_type_ uid;

    // friend_uid
    //
    typedef
    mysql::query_column<
      mysql::value_traits<
        ::size_t,
        mysql::id_ulonglong >::query_type,
      mysql::id_ulonglong >
    friend_uid_type_;

    static const friend_uid_type_ friend_uid;

    // status
    //
    typedef
    mysql::query_column<
      mysql::value_traits<
        ::FriendShipStatus,
        mysql::id_enum >::query_type,
      mysql::id_enum >
    status_type_;

    static const status_type_ status;

    // remark
    //
    typedef
    mysql::query_column<
      mysql::value_traits<
        ::std::string,
        mysql::id_string >::query_type,
      mysql::id_string >
    remark_type_;

    static const remark_type_ remark;

    // created_time
    //
    typedef
    mysql::query_column<
      mysql::value_traits<
        long unsigned int,
        mysql::id_ulonglong >::query_type,
      mysql::id_ulonglong >
    created_time_type_;

    static const created_time_type_ created_time;

    // updated_time
    //
    typedef
    mysql::query_column<
      mysql::value_traits<
        long unsigned int,
        mysql::id_ulonglong >::query_type,
      mysql::id_ulonglong >
    updated_time_type_;

    static const updated_time_type_ updated_time;
  };

  template <typename A>
  const typename query_columns< ::Friendships, id_mysql, A >::id_type_
  query_columns< ::Friendships, id_mysql, A >::
  id (A::table_name, "`id`", 0);

  template <typename A>
  const typename query_columns< ::Friendships, id_mysql, A >::uid_type_
  query_columns< ::Friendships, id_mysql, A >::
  uid (A::table_name, "`uid`", 0);

  template <typename A>
  const typename query_columns< ::Friendships, id_mysql, A >::friend_uid_type_
  query_columns< ::Friendships, id_mysql, A >::
  friend_uid (A::table_name, "`friend_uid`", 0);

  template <typename A>
  const typename query_columns< ::Friendships, id_mysql, A >::status_type_
  query_columns< ::Friendships, id_mysql, A >::
  status (A::table_name, "`status`", 0);

  template <typename A>
  const typename query_columns< ::Friendships, id_mysql, A >::remark_type_
  query_columns< ::Friendships, id_mysql, A >::
  remark (A::table_name, "`remark`", 0);

  template <typename A>
  const typename query_columns< ::Friendships, id_mysql, A >::created_time_type_
  query_columns< ::Friendships, id_mysql, A >::
  created_time (A::table_name, "`created_time`", 0);

  template <typename A>
  const typename query_columns< ::Friendships, id_mysql, A >::updated_time_type_
  query_columns< ::Friendships, id_mysql, A >::
  updated_time (A::table_name, "`updated_time`", 0);

  template <typename A>
  struct pointer_query_columns< ::Friendships, id_mysql, A >:
    query_columns< ::Friendships, id_mysql, A >
  {
  };

  template <>
  class access::object_traits_impl< ::Friendships, id_mysql >:
    public access::object_traits< ::Friendships >
  {
    public:
    struct id_image_type
    {
      unsigned long long id_value;
      my_bool id_null;

      std::size_t version;
    };

    struct image_type
    {
      // id
      //
      unsigned long long id_value;
      my_bool id_null;

      // uid
      //
      unsigned long long uid_value;
      my_bool uid_null;

      // friend_uid
      //
      unsigned long long friend_uid_value;
      my_bool friend_uid_null;

      // status
      //
      mysql::value_traits< ::FriendShipStatus, mysql::id_enum >::image_type status_value;
      unsigned long status_size;
      my_bool status_null;

      // remark
      //
      details::buffer remark_value;
      unsigned long remark_size;
      my_bool remark_null;

      // created_time
      //
      unsigned long long created_time_value;
      my_bool created_time_null;

      // updated_time
      //
      unsigned long long updated_time_value;
      my_bool updated_time_null;

      std::size_t version;
    };

    struct extra_statement_cache_type;

    using object_traits<object_type>::id;

    static id_type
    id (const id_image_type&);

    static id_type
    id (const image_type&);

    static bool
    grow (image_type&,
          my_bool*);

    static void
    bind (MYSQL_BIND*,
          image_type&,
          mysql::statement_kind);

    static void
    bind (MYSQL_BIND*, id_image_type&);

    static bool
    init (image_type&,
          const object_type&,
          mysql::statement_kind);

    static void
    init (object_type&,
          const image_type&,
          database*);

    static void
    init (id_image_type&, const id_type&);

    typedef mysql::object_statements<object_type> statements_type;

    typedef mysql::query_base query_base_type;

    static const std::size_t column_count = 7UL;
    static const std::size_t id_column_count = 1UL;
    static const std::size_t inverse_column_count = 0UL;
    static const std::size_t readonly_column_count = 0UL;
    static const std::size_t managed_optimistic_column_count = 0UL;

    static const std::size_t separate_load_column_count = 0UL;
    static const std::size_t separate_update_column_count = 0UL;

    static const bool versioned = false;

    static const char persist_statement[];
    static const char find_statement[];
    static const char update_statement[];
    static const char erase_statement[];
    static const char query_statement[];
    static const char erase_query_statement[];

    static const char table_name[];

    static void
    persist (database&, object_type&);

    static pointer_type
    find (database&, const id_type&);

    static bool
    find (database&, const id_type&, object_type&);

    static bool
    reload (database&, object_type&);

    static void
    update (database&, const object_type&);

    static void
    erase (database&, const id_type&);

    static void
    erase (database&, const object_type&);

    static result<object_type>
    query (database&, const query_base_type&);

    static unsigned long long
    erase_query (database&, const query_base_type&);

    public:
    static bool
    find_ (statements_type&,
           const id_type*);

    static void
    load_ (statements_type&,
           object_type&,
           bool reload);
  };

  template <>
  class access::object_traits_impl< ::Friendships, id_common >:
    public access::object_traits_impl< ::Friendships, id_mysql >
  {
  };

  // FindFriend
  //
  template <>
  class access::view_traits_impl< ::FindFriend, id_mysql >:
    public access::view_traits< ::FindFriend >
  {
    public:
    struct image_type
    {
      // friend_id
      //
      unsigned long long friend_id_value;
      my_bool friend_id_null;

      // friend_name
      //
      details::buffer friend_name_value;
      unsigned long friend_name_size;
      my_bool friend_name_null;

      // remark
      //
      details::buffer remark_value;
      unsigned long remark_size;
      my_bool remark_null;

      // age
      //
      int age_value;
      my_bool age_null;

      // email
      //
      details::buffer email_value;
      unsigned long email_size;
      my_bool email_null;

      // desc
      //
      details::buffer desc_value;
      unsigned long desc_size;
      my_bool desc_null;

      // avatar
      //
      details::buffer avatar_value;
      unsigned long avatar_size;
      my_bool avatar_null;

      // sex
      //
      mysql::value_traits< ::Sex, mysql::id_enum >::image_type sex_value;
      unsigned long sex_size;
      my_bool sex_null;

      // created_time
      //
      unsigned long long created_time_value;
      my_bool created_time_null;

      // updated_time
      //
      unsigned long long updated_time_value;
      my_bool updated_time_null;

      std::size_t version;
    };

    typedef mysql::view_statements<view_type> statements_type;

    typedef mysql::query_base query_base_type;
    struct query_columns;

    static const bool versioned = false;

    static bool
    grow (image_type&,
          my_bool*);

    static void
    bind (MYSQL_BIND*,
          image_type&);

    static void
    init (view_type&,
          const image_type&,
          database*);

    static const std::size_t column_count = 10UL;

    static query_base_type
    query_statement (const query_base_type&);

    static result<view_type>
    query (database&, const query_base_type&);
  };

  template <>
  class access::view_traits_impl< ::FindFriend, id_common >:
    public access::view_traits_impl< ::FindFriend, id_mysql >
  {
  };

  // Friendships
  //
  // FindFriend
  //
  struct access::view_traits_impl< ::FindFriend, id_mysql >::query_columns
  {
    // Friendships
    //
    typedef
    odb::pointer_query_columns<
      ::Friendships,
      id_mysql,
      odb::access::object_traits_impl< ::Friendships, id_mysql > >
    Friendships;

    // User
    //
    typedef
    odb::pointer_query_columns<
      ::User,
      id_mysql,
      odb::access::object_traits_impl< ::User, id_mysql > >
    User;
  };
}

#include "friend-odb.ixx"

#include <odb/post.hxx>

#endif // FRIEND_ODB_HXX
